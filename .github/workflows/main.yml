name: Hello World

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    steps:
    #- uses: actions/checkout@v2
    - name: Run pwd
      run: pwnd
    - name: Run ls -al
      run: ls -al
    
    
